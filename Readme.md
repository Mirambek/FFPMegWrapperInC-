This ffmpeg wrapper converter (scale only). The full solution used with Docker,RabbitMQ, FFMPEG third party application. 

Please refer dockerfile for running application. The commands are commented out (#).
